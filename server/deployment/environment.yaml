name: server
channels:
  - defaults
dependencies:
  - pip
  - pip:  # Same as requirements.txt
    - cohere
    - fastapi
    - fastapi_utils # Lets FastAPI run background tasks
    - httpx
    - pillow
    - supabase
    - tqdm
    - uvicorn[standard]
